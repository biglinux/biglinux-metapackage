# Maintainer: Bruno Goncalves <bigbruno@gmail.com>

pkgname=biglinux-metapackage
pkgver=$(date +%y.%m.%d)
pkgrel=$(date +%H%M)
arch=('any')
license=('GPL')
depends=('bigbashview' 'xorg-fonts-100dpi' 'hplip' 'patch' 'usb-dirty-pages-udev' 'power-profiles-daemon-biglinux')
depends_x86_64=('render-menu-options')
# conflicts=('')
pkgdesc="Meta package to simplify BigLinux updates"
replaces=('kernel-alive')

package() {
  true
}
